class Cells {
    constructor (x, y, w) {
        this.cellX = x;
        this.cellY = y;
        this.cellW = w;

    }

    display(offsetX, offsetY) {
    this.x = offsetX;
    this.y = offsetY 
        // noFill();
        // stroke(200);
        // strokeWeight(1);
        // rect(this.x, this.y, this.cellW, this.cellW);
    }
}

