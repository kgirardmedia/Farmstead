let playerR;


class Collision {
    constructor(playerR, y, w, h) {
        this.type = type;
        this.x = x;
        this.y = y;
        this.h = h;
        this.w = w;
        this.playerX = Character.CharacterPosX;
        this.playerY = Character.CharacterPosY;
    }
    detectCollision() {
            if (type == 'tree') {

            }
    }
}
